# 🎉 Automated Birthday Email using GitHub Actions

The respository contains a GitHub Actions workflow that automatically sends a "Happy Birthday" email at 00:00 IST to a specific recipient on their birthday.

## 🚀 How It Works
- The workflow runs at 00:00 IST (18:30 UTC) using GitHub Actions.
- It uses the SMTP protocol to send an email.

## 📌 Prerequisites
Before setting up the workflow, ensure you have:
1. A GitHub repository
2. A Gmail account (or another SMTP-supported email provider)
3. GitHub secrets configures with SMTP credentials.

## 🔧 Setup Instructions
1. Enable app password for Gmail
2. Add secrets to GitHub repository
